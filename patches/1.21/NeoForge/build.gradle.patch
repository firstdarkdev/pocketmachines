--- a/NeoForge/build.gradle
+++ b/NeoForge/build.gradle
@@ -15,12 +15,8 @@
  */
 
 unimined.minecraft {
-    neoForge {
-        if (isPort) {
-            loader "net.neoforged:neoforge:${neoforge_version}:universal"
-        } else {
-            loader neoforge_version
-        }
+    neoForged {
+        loader neoforge_version
         mixinConfig("${mod_id}.mixins.json")
     }
 }
@@ -53,8 +49,8 @@
     setVersionType("release")
     setChangelog("https://raw.githubusercontent.com/hypherionmc/changelogs/refs/heads/main/pocketmachines.md")
     setProjectVersion("${minecraft_version}-${project.version}")
-    setDisplayName("[NeoForge 1.21.2] Simple Pocket Machines - ${project.version}")
-    setGameVersions("1.21.2")
+    setDisplayName("[NeoForge 1.21.1] Simple Pocket Machines - ${project.version}")
+    setGameVersions("1.21.1")
     setLoaders("neoforge")
     setArtifact(remapJar)
     setCurseEnvironment("both")
